apiVersion: v1
kind: Service
metadata:
  name: {{ include "legacy-app.fullname" . }}
  labels:
    {{- include "legacy-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "legacy-app.selectorLabels" . | nindent 4 }}
